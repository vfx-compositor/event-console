# 기여 안내

Event Console에 관심을 가져 주셔서 감사합니다. 현재 구현은 실제 행사에서 사용한 4팀 운영 규칙을 담고 있으므로, 동작을 바꿀 때는 화면뿐 아니라 점수·진행·복구 계약까지 함께 확인해야 합니다.

## 개발 환경

- Node.js 22.12 이상
- npm 10 이상
- 최신 Chrome 또는 Edge

```bash
npm ci
npm test
npm run build
```

기능을 개발할 때는 `npm run dev`를 사용하고, 제출 전에는 위 검증 명령을 새로 실행해 주세요.

## 변경 원칙

- 상태 변경은 `src/state.ts`의 action과 reducer 경계를 따릅니다.
- 점수는 원장 항목에서 계산하며 화면 숫자를 별도 정본으로 만들지 않습니다.
- 조작 화면과 송출 화면의 같은 origin·같은 프로필 동기화 계약을 유지합니다.
- 행사 규칙을 바꾸면 `src/cue.ts`, `src/scoring.ts`, 관련 장면과 테스트를 함께 확인합니다.
- 사용자 입력, 키보드 조작, 포커스, 색상 외 상태 표시와 같은 접근성 동작을 보존합니다.
- 새 런타임 의존성은 꼭 필요한 경우에만 추가하고 이유를 변경 설명에 남깁니다.

## 테스트

테스트는 소스 가까이에 `*.test.ts`로 둡니다. 회귀를 고칠 때는 같은 원인의 동급 동작을 확인하는 테스트를 추가하세요. 브라우저 API는 가능한 작은 순수 함수 경계에서 검증하고, 실제 카메라·오디오·전체화면 동작은 Chrome 또는 Edge 리허설 결과도 함께 기록합니다.

## 미디어와 개인정보

행사 영상·음악·사진, 참가자 정보, API 키, 로컬 경로가 담긴 파일을 커밋하지 마세요. 테스트에는 직접 만든 작은 fixture나 권리 관계가 명확한 자료만 사용합니다. `public/media/`, `public/music/`, `public/photo-seed/`의 실제 미디어는 기본적으로 Git에서 제외됩니다.

## 문서와 변경 설명

사용자 실행법이 달라지면 `README.md`를, 구조나 데이터 흐름이 달라지면 `docs/ARCHITECTURE.md`를 함께 갱신합니다. 변경 설명에는 문제, 원인, 핵심 변경, 검증 결과와 남은 제한을 적어 주세요.

기여된 애플리케이션 코드는 저장소의 MIT License로 배포될 수 있어야 합니다. 제3자 코드·폰트·이미지를 추가할 때는 원 저작권 고지와 배포 조건을 함께 포함하세요.
